{
  "name": "tlg",
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs",
    "typecheck": "pnpm cf:types && tsc --noEmit",
    "cf:types": "pnpm wrangler types ./wrangler.d.ts",
    "cf:deploy": "pnpm cf:types && pnpm build && pnpm wrangler deploy",
    "cf:db:generate": "pnpm drizzle-kit generate",
    "cf:db:migrate": "pnpm wrangler d1 migrations apply tlg"
  },
  "devDependencies": {
    "@types/node": "^25.0.6",
    "drizzle-kit": "^0.31.8",
    "eslint": "^9.39.2",
    "nitro-cloudflare-dev": "^0.2.2",
    "nitropack": "^2.13.0",
    "wrangler": "^4.58.0"
  },
  "dependencies": {
    "@grammyjs/menu": "^1.3.1",
    "db0": "^0.3.4",
    "drizzle-orm": "^0.45.1",
    "grammy": "^1.39.2",
    "h3": "1.15.4"
  }
}